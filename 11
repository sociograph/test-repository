# Автори: Кравченко, Катрій
#
# Цей код створює тестовий набір даних з трьох категорій, кожна з яких містить 100 випадкових значень,
# що розподілені за нормальним законом з різними середніми значеннями та стандартними відхиленнями.
# Потім він обчислює для кожної категорії основні статистичні показники: середнє значення, стандартне 
# відхилення, мінімум і максимум. Після цього виводить ці статистики на екран та створює коробковий графік
# для візуалізації розподілу значень за категоріями.

set.seed(123)  # Фіксуємо початкове значення генератора випадкових чисел для відтворюваності результатів

Category <- rep(c("A", "B", "C"), each = 100)
Value <- c(
  rnorm(100, mean = 5, sd = 1),
  rnorm(100, mean = 7, sd = 1.5),
  rnorm(100, mean = 6, sd = 1.2)
)
data <- data.frame(Category, Value)

summary_stats <- aggregate(Value ~ Category, data = data, function(x) c(Mean = mean(x), SD = sd(x), Min = min(x), Max = max(x)))
print(summary_stats)

boxplot(Value ~ Category, data = data,
        main = "Розподіл значень за категоріями",
        xlab = "Категорія",
        ylab = "Значення",
        col = c("lightblue", "lightgreen", "lightcoral"))
